Drop build-in-public photos here with these exact filenames — the site already points to them, so uploading a file with the right name is all that's needed (no rebuild required):

- `bip-1.jpg` — Dự án đang làm / Current Build
- `bip-2.jpg` — Hậu trường / Behind the Scenes

`bip-0` (Latest Video) is fetched automatically from YouTube and has no static image.
