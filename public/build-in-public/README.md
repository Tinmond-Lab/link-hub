Build-in-public photos, referenced directly by `src/pages/index.astro`:

- `bip-1.webp` — Dự án đang làm / Current Build
- `bip-2.webp` — Hậu trường / Behind the Scenes

`bip-0` (Latest Video) is fetched automatically from YouTube and has no static image.

Replacing a photo later? Uploading a file with the same name is enough — no rebuild required. Keep it `.webp` and compressed (~80% quality, max ~1200px wide) — photos straight from a camera are typically several MB each.
